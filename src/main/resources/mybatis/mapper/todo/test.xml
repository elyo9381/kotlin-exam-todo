<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springdemo.mapper.TodoSqlMapper">

    <select id="SelectAllList" parameterType="com.example.springdemo.dto.TodoPageDto"
            resultType="com.example.springdemo.dto.TodoDto">
        <![CDATA[
        select * from todo_entity t
        where true
        ]]>
        <if test='title != null and title !=""'>
            and title like concat('%', #{title}, '%')
        </if>
        <if test='description != null and description !=""'>
            and description like concat('%', #{description}, '%')
        </if>

        order by id asc
        limit #{start} , #{limit}
    </select>

    <select id="testTableCount" parameterType="com.example.springdemo.dto.TodoPageDto" resultType="long">
        <![CDATA[
        SELECT count(*) FROM todo_entity t where true
        ]]>
            <if test='title != null and title != ""'>
                and title LIKE CONCAT('%',#{title},'%')
            </if>
            <if test='description != null and description != ""'>
                and description LIKE CONCAT('%',#{description},'%')
            </if>

    </select>


</mapper>

